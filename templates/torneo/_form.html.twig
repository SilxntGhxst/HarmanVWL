{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
    {{ form_widget(form) }}
    <div class="form-actions">
        <button class="btn-primario">{{ button_label|default('general.save'|trans) }}</button>
    </div>
{{ form_end(form) }}
