{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
    {{ form_widget(form) }}
    <div class="mt-6 flex justify-end">
        <button class="btn-primario">{{ button_label|default('Guardar') }}</button>
    </div>
{{ form_end(form) }}
