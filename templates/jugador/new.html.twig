{% extends 'base.html.twig' %}

{% block title %}{{ 'jugador.new.title'|trans }}{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('app_jugador_index') }}" class="link-back">
            <span>&larr;</span> {{ 'general.back_to_list'|trans }}
        </a>
    </div>

    <div class="card max-w-2xl mx-auto">
        <div class="card-header">
            <h1 class="card-title">{{ 'jugador.new.title'|trans }}</h1>
        </div>

        <div class="mt-4">
            {{ include('jugador/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
