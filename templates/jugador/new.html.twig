{% extends 'base.html.twig' %}

{% block title %}Nuevo Jugador{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('app_jugador_index') }}" class="text-gray-400 hover:text-green-400 transition-colors text-sm flex items-center gap-2">
            <span>&larr;</span> Volver a la lista
        </a>
    </div>

    <div class="card max-w-2xl mx-auto">
        <div class="card-header">
            <h1 class="text-2xl font-bold text-white">Crear Nuevo Jugador</h1>
        </div>

        <div class="mt-4">
            {{ include('jugador/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
